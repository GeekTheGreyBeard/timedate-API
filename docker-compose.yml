services:
  timedate-app:
    build: .
    container_name: timedate-app
    ports:
      - "8200:8200"
    restart: always
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8200/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s 
